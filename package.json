{
  "name": "ton",
  "scripts": {
    "dev": "bun run --filter 'web' dev",
    "build": "bun run --filter 'web' build",
    "ts": "bun run --watch scripts/run.ts"
  },
  "workspaces": [
    "web"
  ],
  "dependencies": {
    "ronin": "^4.3.1"
  },
  "devDependencies": {
    "@ronin/drophunt": "^0.0.0-3453496097762"
  },
  "prettier": {
    "semi": false
  }
}
