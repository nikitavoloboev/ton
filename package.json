{
  "name": "ton",
  "scripts": {
    "dev": "bun run --filter 'web' dev",
    "build": "bun run --filter 'web' build"
  },
  "workspaces": [
    "web"
  ],
  "dependencies": {
    "ronin": "^4.3.1"
  }
}
